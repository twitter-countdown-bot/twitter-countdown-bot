name: Schedule Test

on:
  schedule:
    - cron: '40 19 * * *' # 1:10 AM IST

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World! The schedule is working."
